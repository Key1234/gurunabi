<%= form_for(posting) do |f| %>
  <% if posting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(posting.errors.count, "error") %> prohibited this posting from being saved:</h2>

      <ul>
      <% posting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions posting-submit">
    <%= f.submit %>
  </div>

  <div class="field posting-user-name">
    <%= f.label :user_name %>
    <%= f.text_field :user_name %>
  </div>

  <div class="field posting-text">
    <%= f.text_area :text %>
  </div>

<% end %>
